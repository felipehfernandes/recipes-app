import React from 'react';

import Header from '../components/Header';

function RecipesCompleted() {
  return (
    <div>
      <Header pageTitle="Done Recipes" searchBtn={ false } />
    </div>
  );
}

export default RecipesCompleted;
